```bash
# Macbook
install chrome
install atom
install xcode
install brew
install iterm
install hack font
install spectacle
install vlc
install slack
install dropbox
install google-drive

# Brew
brew install macvim --with-override-system-vim
brew install yarn
brew install ruby
brew install cmake
brew install tmux
brew install wget

git clone vundle
```
